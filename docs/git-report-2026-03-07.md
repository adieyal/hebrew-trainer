# Git Report

Date: 2026-03-07

## Repository status

This project was not yet initialized as a Git repository. The initial repository setup should track the application source, project documentation, and package configuration while excluding generated artifacts and local caches.

## Files selected for version control

- `src/` application source, domain logic, storage adapters, and tests
- `docs/plans/` project planning notes created on 2026-03-07
- `index.html` Vite entry document
- `package.json` and `package-lock.json` dependency manifest and lockfile
- `spec.md` product specification
- `tsconfig*.json` TypeScript project configuration
- `vite.config.ts` Vite configuration

## Files excluded from version control

- `node_modules/` installed dependencies
- `dist/` generated production build output
- `backups/` local backup exports
- `*.tsbuildinfo` incremental compiler caches
- `vite.config.js` and `vite.config.d.ts` generated config artifacts derived from the TypeScript config source

## Initial commit intent

Create a clean initial commit for the Hebrew trainer project with source code, docs, and configuration only.
